@model IEnumerable<TalentManagement.Models.Gig>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GigDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VenueName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArtisteId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IncomeGenerated)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GigDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VenueName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArtisteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncomeGenerated)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GigsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GigsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GigsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
